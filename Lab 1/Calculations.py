from math import *

print (cos(0))
print (sin(0)) # python works in radians
# If you divide by 0 it returns an error
# The rest of the program won't run if the line is at the beginning
# the program runs until it gets to the line that divides by 0
